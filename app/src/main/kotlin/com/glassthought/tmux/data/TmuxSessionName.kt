package com.glassthought.tmux.data

/**
 * Represents a tmux session that has been created.
 *
 * @param sessionName The name used to identify the session in tmux.
 */
data class TmuxSessionName(val sessionName: String)