---
spec: "com.glassthought.shepherd.core.agent.facade.FakeAgentFacadeTest"
status: PASSED
failed: 0
skipped: 0
---

- GIVEN a DispatcherProvider backed by StandardTestDispatcher
  - WHEN a coroutine is launched on the injected dispatcher
    - [PASS] THEN the coroutine runs on the test dispatcher
- GIVEN a FakeAgentFacade (default kill behavior)
  - WHEN killSession is called
    - [PASS] THEN the call is recorded
    - [PASS] THEN the recorded handle matches what was passed
- GIVEN a FakeAgentFacade programmed to return Crashed
  - WHEN sendPayloadAndAwaitSignal is called
    - [PASS] THEN it returns Crashed with the programmed details
- GIVEN a FakeAgentFacade programmed to return Done(COMPLETED)
  - WHEN sendPayloadAndAwaitSignal is called
    - [PASS] THEN it returns Done(COMPLETED)
    - [PASS] THEN the call is recorded with handle and payload
- GIVEN a FakeAgentFacade programmed to return FailWorkflow
  - WHEN sendPayloadAndAwaitSignal is called
    - [PASS] THEN it returns FailWorkflow with the programmed reason
- GIVEN a FakeAgentFacade programmed to return SelfCompacted
  - WHEN sendPayloadAndAwaitSignal is called
    - [PASS] THEN it returns SelfCompacted
- GIVEN a FakeAgentFacade programmed with 75% remaining context
  - WHEN readContextWindowState is called
    - [PASS] THEN it returns 75% remaining
    - [PASS] THEN the call is recorded
- GIVEN a FakeAgentFacade programmed with null remaining (stale)
  - WHEN readContextWindowState is called
    - [PASS] THEN remainingPercentage is null
- GIVEN a FakeAgentFacade programmed with sequential signals
  - WHEN sendPayloadAndAwaitSignal is called twice
    - [PASS] THEN the first call returns NEEDS_ITERATION
    - [PASS] THEN the second call returns COMPLETED
- GIVEN a FakeAgentFacade with NO readContextWindowState programmed
  - WHEN readContextWindowState is called
    - [PASS] THEN it throws IllegalStateException (fail hard)
- GIVEN a FakeAgentFacade with NO sendPayloadAndAwaitSignal programmed
  - WHEN sendPayloadAndAwaitSignal is called
    - [PASS] THEN it throws IllegalStateException (fail hard)
- GIVEN a FakeAgentFacade with NO spawn behavior programmed
  - WHEN spawnAgent is called
    - [PASS] THEN it throws IllegalStateException (fail hard)
- GIVEN a FakeAgentFacade with no calls made
  - [PASS] THEN killSessionCalls is empty
  - [PASS] THEN readContextWindowStateCalls is empty
  - [PASS] THEN sendPayloadCalls is empty
  - [PASS] THEN spawnCalls is empty
- GIVEN a FakeAgentFacade with programmed spawn behavior
  - WHEN spawnAgent is called
    - [PASS] THEN it returns the pre-programmed handle
    - [PASS] THEN the recorded config matches what was passed
    - [PASS] THEN the spawn call is recorded
- GIVEN runTest with TestClock
  - WHEN both TestClock and coroutine virtual time are advanced
    - [PASS] THEN TestClock advances independently of coroutine virtual time
- GIVEN runTest with a FakeAgentFacade simulating delay
  - WHEN advanceTimeBy is used to fast-forward past the delay
    - [PASS] THEN the coroutine completes without real wall-clock wait
