---
spec: "com.glassthought.shepherd.usecase.healthmonitoring.TicketFailureLearningUseCaseImplTest"
status: PASSED
failed: 0
skipped: 0
---

- GIVEN agent instructions assembly
  - WHEN assembleAgentInstructions is called
    - [PASS] THEN includes artifacts directory path
    - [PASS] THEN includes expected output format
    - [PASS] THEN includes failure facts
- GIVEN agent returns Failed result
  - WHEN recordFailureLearning is called
    - [PASS] THEN still commits to try branch
    - [PASS] THEN still includes structured facts
    - [PASS] THEN uses fallback summary
- GIVEN agent succeeds (happy path)
  - WHEN recordFailureLearning is called with FailedWorkflow
    - [PASS] THEN appends TRY-N section with Previous Failed Attempts heading
    - [PASS] THEN commits on the try branch
    - [PASS] THEN includes TRY-1 sub-heading
    - [PASS] THEN includes agent-generated summary
    - [PASS] THEN includes branch name in structured facts
    - [PASS] THEN includes failed-at and iteration
    - [PASS] THEN includes failure type derived from PartResult variant
    - [PASS] THEN includes parts completed
    - [PASS] THEN includes workflow type
    - [PASS] THEN preserves original ticket content
    - [PASS] THEN propagates to originating branch
- GIVEN agent times out
  - WHEN recordFailureLearning is called
    - [PASS] THEN does not include agent output in summary section
    - [PASS] THEN uses fallback summary
- GIVEN buildTrySection
  - WHEN partsCompleted is empty
    - [PASS] THEN shows 'none' for parts completed
- GIVEN each PartResult variant
  - WHEN mapping to failure context
    - [PASS] THEN AgentCrashed maps to failureType AgentCrashed
    - [PASS] THEN Completed maps to failureType Completed
    - [PASS] THEN FailedToConverge maps to failureType FailedToConverge
    - [PASS] THEN FailedWorkflow maps to failureType FailedWorkflow
    - [PASS] THEN failedAt comes from run context
    - [PASS] THEN iteration comes from run context
    - [PASS] THEN partsCompleted comes from run context
    - [PASS] THEN workflowType comes from run context
- GIVEN git commit fails on try branch
  - WHEN recordFailureLearning is called
    - [PASS] THEN does not throw (non-fatal)
    - [PASS] THEN still updates the ticket file
- GIVEN propagation to originating branch fails
  - WHEN recordFailureLearning is called
    - [PASS] THEN does not throw (non-fatal)
    - [PASS] THEN still has ticket updated with TRY section
- GIVEN ticket already has Previous Failed Attempts heading
  - WHEN recordFailureLearning is called
    - [PASS] THEN appends new TRY-N under existing heading without duplicating it
    - [PASS] THEN preserves existing TRY section
