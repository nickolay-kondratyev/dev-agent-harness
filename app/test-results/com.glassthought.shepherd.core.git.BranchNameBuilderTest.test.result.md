---
spec: "com.glassthought.shepherd.core.git.BranchNameBuilderTest"
status: PASSED
failed: 0
skipped: 0
---

- GIVEN build
  - WHEN called with a long title and id='nid_abc123'
    - [PASS] THEN branch name ends with '__try-1'
    - [PASS] THEN branch name starts with 'nid_abc123__'
    - [PASS] THEN the slug portion contains a hash suffix
    - [PASS] THEN the slug portion is at most 50 characters
  - WHEN called with blank id
    - [PASS] THEN throws IllegalArgumentException
  - WHEN called with id='TK-001', title='My Feature', tryNumber=1
    - [PASS] THEN returns 'TK-001__my-feature__try-1'
  - WHEN called with id='TK-001', title='My Feature', tryNumber=3
    - [PASS] THEN returns 'TK-001__my-feature__try-3'
  - WHEN called with tryNumber=-1
    - [PASS] THEN throws IllegalArgumentException
  - WHEN called with tryNumber=0
    - [PASS] THEN throws IllegalArgumentException
- GIVEN slugify
  - WHEN called twice with the same long input
    - [PASS] THEN returns identical results
  - WHEN called with a long multi-word title that exceeds 50 chars
    - [PASS] THEN result does not end with a hyphen
    - [PASS] THEN result is at most 50 chars
    - [PASS] THEN result uses character-level fallback since first word exceeds 43 chars
  - WHEN called with a simple title 'My Feature'
    - [PASS] THEN returns 'my-feature'
  - WHEN called with a single long word of 60 chars
    - [PASS] THEN result ends with hash suffix
    - [PASS] THEN result is exactly 50 chars
    - [PASS] THEN result starts with 43 chars of the word
  - WHEN called with a single long word of 80 'a' chars
    - [PASS] THEN result is exactly 50 chars
    - [PASS] THEN result uses character-level fallback with hash
  - WHEN called with a title that slugifies to exactly 50 chars
    - [PASS] THEN returns the full slug without hash suffix
  - WHEN called with a title that slugifies to exactly 51 chars
    - [PASS] THEN result ends with a 6-char hex hash suffix
    - [PASS] THEN result is exactly 50 chars
    - [PASS] THEN result matches expected value
    - [PASS] THEN result starts with 43 'a' chars
  - WHEN called with an empty string
    - [PASS] THEN returns 'untitled'
  - WHEN called with any truncated slug
    - [PASS] THEN result is at most 50 chars
    - [PASS] THEN result never ends with a hyphen
  - WHEN called with consecutive spaces 'fix the bug'
    - [PASS] THEN returns 'fix-the-bug'
  - WHEN called with first word exactly 43 chars followed by more words
    - [PASS] THEN result is exactly 50 chars
    - [PASS] THEN result keeps the 43-char first word and appends hash
  - WHEN called with leading/trailing special chars '---hello---'
    - [PASS] THEN returns 'hello'
  - WHEN called with only special characters '!@#$%^'
    - [PASS] THEN returns 'untitled'
  - WHEN called with special characters 'Fix: bug #123!'
    - [PASS] THEN returns 'fix-bug-123'
  - WHEN called with the spec canonical example
    - [PASS] THEN result is 46 chars
    - [PASS] THEN returns word-boundary truncated slug with hash suffix
  - WHEN called with two similar long titles sharing a prefix
    - [PASS] THEN produces different slugs
    - [PASS] THEN slugA has expected hash suffix
    - [PASS] THEN slugB has expected hash suffix
  - WHEN called with unicode characters 'café latté'
    - [PASS] THEN non-ascii chars become hyphens producing 'caf-latt'
