Exact string replacement.
- You must use your `Read` tool at least once in the conversation before editing. This tool will error if you attempt an edit without reading the file.  Preserve exact indentation from output (ignore line number prefix). Fails if old_string not unique - add context or use replace_all. Prefer editing over creating new files.
